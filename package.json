{
  "name": "container-local-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox extension for local-only proxy settings per container identity.",
  "scripts": {
    "test": "node --test",
    "lint:manifest": "node scripts/validate-manifest.js",
    "build": "node scripts/build.js",
    "check": "npm run test && npm run lint:manifest && npm run build"
  },
  "devDependencies": {
    "web-ext": "^8.3.0"
  }
}
